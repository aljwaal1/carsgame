class RetroRoadConfig {
  static const double playerY = 0.82;

  // Much slower than the first prototype. The old game became too fast too early.
  static const double initialSpeed = 0.0068;
  static const double maxSpeed = 0.020;

  static const int carsPerDay = 40;
  static const int pointsPerRoadTick = 1;
  static const int pointsPerPass = 100;
  static const int pointsPerDayClear = 750;
}
